{{ if .Values.service.port }}
kind: Service
apiVersion: v1
metadata:
  name: svc-{{ .Chart.Name }}
  labels:
    app: {{ .Chart.Name }}
    type: service
    bound: internal
spec:
  selector:
    app: {{ .Chart.Name }}
    type: service
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
{{ end }}